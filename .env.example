# Point the dashboard at your Strolium backend (no trailing slash, no /api).
# If you don't set this, it defaults to the Railway domain in src/config.js.
VITE_API_BASE=https://web-production-18d55.up.railway.app
